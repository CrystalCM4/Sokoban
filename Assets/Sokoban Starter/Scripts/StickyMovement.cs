using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StickyMovement : Movement
{

    /*
    if an object is next to it
    check if it can move (up/down/left/right booleans)
    if a boolean is true, it will go that way
    if there are multiple true booleans, it will depend on keypress (WASD)

    prevent movement if

    */


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
